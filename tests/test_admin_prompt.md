我现在正在实现一个AI语音克隆提供网站，其中:

- `voiceclone-pro-console`是前端代码库，负责用户界面和交互。
- `backend`是后端代码库，处理API请求和数据管理。
- `tests`包含当前的自动化测试脚本和当前测试状态。
- `run_frontend_and_backend.sh`是一个脚本，你可以使用`run_frontend_and_backend.sh start`来启动前端和后端服务。使用`run_frontend_and_backend.sh stop`来停止它们。
你需要忽略`.gitignore`文件中列出的文件和目录。

 TEST OBJECTIVE

编写一个名为 `admin_dashboard_management.spec.ts` 的测试文件，覆盖管理员对用户资产、系统合规性及资源监控的管理功能。你需要保证你的测试符合

 测试用例定义

<test_admin_1: 权限隔离与用户资产管理>

1. 越权测试:
   - 用户使用 `xiaowu.417@qq.com`, 密码`1234qwer`登录。
   - 尝试直接访问 URL `/admin/dashboard` 或 `/admin/users`。
   - 验证:系统应拦截并重定向至首页，或显示 403 无权限错误。
2. 管理员登录:
   - 使用管理员账号 `admin@voiceclone.pro`，密码 `admin1234` 登录系统。
   - 验证:成功进入后台界面，侧边栏显示“用户管理”、“音色审核”、“系统监控”。
3. 积分操纵:
   - 在“用户管理”页面搜索 `xiaowu.417@qq.com`。
   - 记录当前积分余额。
   - 点击“手动调账”，为该用户增加 `100` 积分。
   - 验证:列表显示积分已实时更新，且数据库中产生一条对应的 `admin_log` 记录。
4. 账户运维:
   - 为该用户重置密码为 `reset_123456`。
   - 验证:管理员登出后，使用该用户原密码无法登录，使用新密码可成功登录。
</test_admin_1>

<test_admin_2: 音色合规审计与物理清理>
1. 跨用户资源查看:
   - 使用`xiaowu.417@qq.com`, 密码`1234qwer`登录，选择上传`/home/xiaowu/voice_web_app/data/audio/季冠霖语音包.MP3`,进行声音克隆，生成音色`test_admin_2`。
   - 用户登出。
   - 使用管理员账号 `admin@voiceclone.pro`，密码 `admin1234` 登录系统。
   - 管理员进入“音色审计”列表。
   - 验证:管理员可以看到所有用户克隆的音色，可以看到用户生成的`test_admin_2` 音色，包括了生成日期和该音色的使用次数。
   - 验证:管理员点击该音色的"使用详情"按钮，可以看到用户使用该音色生成的所有语音记录列表，包括生成时间和文本内容，生成格式，下载链接等信息。
   - 验证:管理员点击某条生成记录的下载链接，可以成功下载该语音文件，且文件内容正确。

2. 违规删除 (级联清理):
   - 使用管理员账号 `admin@voiceclone.pro`，密码 `admin1234` 登录系统。
   - 管理员针对某个违规音色点击“彻底删除”。
   - 验证:
     - 调用 Fish-Audio API 成功删除远程模型（不包括系统预定义）。同时，创建该音色对应的音频文件，和使用该音色生成的所有语音文件在阿里云 OSS 中被物理删除。
     - 前端验证: "声音库"的音色列表中不再显示该音色，"语音生成"的最近上传文件中创建该音色对应的音频文件不再显示该文件，用户历史生成记录中也不再显示该音色的使用记录。
     - 后端验证：后端数据库中该*音色记录*，*创建该音色使用的音频文件记录*和*使用该音色生成的所有语音文件记录*被移除。
     - OSS 验证:验证对应的存储文件已在阿里云 OSS 中被物理删除。
     - 数据保护:验证该操作未修改用户的 `seen_location` 历史访问轨迹记录。
</test_admin_2>

<test_admin_3: 系统资源监控>

1. fish-audio 资源使用监控:
   - 使用管理员账号 `admin@voiceclone.pro`，密码 `admin1234` 登录系统。
   - 管理员进入"系统监控"-->"API使用"页面。
   - 验证：
        - 页面显示了当前后端fish-audio API的使用情况，包括余额，已用额度(时间跨度分为当小时、当日、当月)。
   你需要尽可能多的参考`/home/xiaowu/voice_web_app/fish-audio-doc/`里的文档和fetch文档内的URL来实现这个测试。

2. 系统资源看板:
   - 管理员进入"系统监控" --> "资源看板"页面。
   - 验证:
     - 页面显示了当前系统的CPU使用率、内存使用率、磁盘使用率等关键指标。
     - 页面显示了最近24小时内的系统负载趋势图表。
     - 页面显示了阿里云OSS存储使用情况，包括总存储量和已用存储量，以及阿里云账户余额。
     - 页面显示了网站的流量带宽使用情况，包括峰值带宽和平均带宽。
</test_admin_3>

<test_admin_4: 财务对账与充值流水审计>
1. 全站流水穿透查询:
   - 管理员进入“财务管理”-->“充值记录”页面。
   - 搜索特定时间段或特定用户 `xiaowu.417@qq.com` 的所有支付订单。
   - 验证: 列表准确显示订单号、支付渠道（微信/支付宝/Stripe）、支付状态及到账积分。
2. 手动补单功能:
   - 管理员针对一条“支付中”或“异常”的订单，点击“手动确认到账”。
   - 验证:
     - 订单状态变为 `Completed`。
     - 用户 `xiaowu.417@qq.com` 的积分余额自动累加。
     - 审计日志中记录该“手动补单”操作的管理员 ID 及原因备注。
3. 财务报表统计:
   - 管理员查看“营收概览”看板。
   - 验证: 页面显示的“今日总收入”与“积分消耗总额”在数值逻辑上与用户流水统计一致。
</test_admin_4>

<test_admin_5: 运营配置与内容风控>

1. 系统公告发布:
   - 管理员在“运营中心”编写一条新公告：“系统维护通知”，设置展示时间。
   - 验证: 以普通用户 `xiaowu.417@qq.com` 登录后，首页顶端能够准确显示该公告内容。
2. 敏感词库管理:
   - 管理员在“风控配置”中添加敏感词 `[诈骗, 广告]` 到 TTS 过滤名单。
   - 验证: 模拟用户在工作台输入包含上述敏感词的文本并点击生成。
   - 验证: 系统拦截请求并提示“包含违规内容”，且管理员后台生成一条“拦截触发记录”。
3. 预定义音色更新:
   - 管理员在“公共声音库”中新增一个预设音色（从 Fish-Audio 系统声音中导入）。
   - 验证: 所有普通用户在“克隆/音色选择”界面均能看到并试听该新增的公共音色。
</test_admin_5>

<test_admin_6: 安全审计与操作溯源>
1. 敏感操作日志记录:
   - 管理员执行一次“删除用户”或“修改全局费率”的操作。
   - 验证: 进入“安全审计”页面，确认产生了一条类型为 `CRITICAL` 的日志。
   - 验证: 日志详情包含：操作 IP、设备信息、操作前后的数据快照对比。
2. 管理员多级权限验证:
   - 使用一个仅有“运营权限”的账号（非超级管理员）登录。
   - 尝试执行“删除数据库备份”或“修改其他管理员权限”的操作。
   - 验证: 界面隐藏相关按钮，或在提交请求时返回 `Permission Denied`。
</test_admin_6>

分析当前的代码，修改代码保证所有测试样例都能通过。